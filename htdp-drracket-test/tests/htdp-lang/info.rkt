#lang info

(define test-timeouts '(("test-engine-test.rkt" 300)))

(define test-responsibles '(("test-engine-test.rkt" sperber)
                            ("language-test.rkt" (robby matthias))
                            (all robby)))
